# Core ML Framework
# Note: install a CUDA-enabled PyTorch build for NVIDIA GPUs (see README).
torch>=2.0.0
torchvision>=0.15.0

# Data Processing
numpy>=1.24.0
pillow>=9.5.0
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0
tensorboard>=2.13.0

# Optional but recommended for better performance
tqdm>=4.65.0
